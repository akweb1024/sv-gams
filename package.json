{
  "name": "shoorveer-game",
  "version": "1.0.0",
  "description": "Multi-dimensional online game inspired by Shoorveer",
  "scripts": {
    "server": "cd server && node src/index.js",
    "server:dev": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "build:client": "cd client && npm run build",
    "start:prod": "cd server && node src/index.js",
    "prisma:migrate:deploy": "cd server && npx prisma migrate deploy",
    "seed": "cd server && node src/utils/seed.js",
    "setup": "cd server && npm install && npx prisma generate && npx prisma migrate deploy && node src/utils/seed.js && cd ../client && npm install"
  }
}
